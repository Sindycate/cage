<!-- Canonical project instructions live in AGENTS.md (read by both Claude Code and
     Codex CLI). This file just imports them so Claude Code picks them up too.
     Add any Claude-specific instructions below the import. -->
@AGENTS.md
